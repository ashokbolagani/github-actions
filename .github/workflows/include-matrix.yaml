name: include-matrix
on: workflow_dispatch
jobs:
  testing-job:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest]
        node: [16]
        include:
          - os: ubuntu-latest
            node: 18
            message: optimized-build
    steps:
      - run: |
          echo "print my name ln"
          echo "print ashok"
      
