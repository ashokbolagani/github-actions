name: step-level-envvarible
on: 
  workflow_dispatch:
jobs:
  test-job:
    runs-on: ubuntu-latest
steps:
  - name: en-var 
    env:
      king: ashok
      city: repalle
    run: |
      echo "the jungle king is $king from $city"
      echo "never compare with $king is dangaer from $city:"
      
      
  
  
