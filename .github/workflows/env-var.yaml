name: step-level-envvarible
on: 
  workflow_dispatch:
jobs:
  test-job:
    runs-on: ubuntu-latest
    steps:
      - name: step-level
        env:
          king: ashok
          city: repalle
        run: |
           echo "the jungle king is $king from $city"
           echo "never compare with $king is danger from $city"
           
      - name: step-level2
        run: echo "king is $king city is $city"
      
  
  
